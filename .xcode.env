# Used by Xcode when running script phases (e.g. Bundle React Native code).
# See: https://reactnative.dev/docs/environment-setup

export NODE_BINARY=$(command -v node)

# Стандартный порт 8081 для совместимости с Android
export RCT_METRO_PORT=8081
